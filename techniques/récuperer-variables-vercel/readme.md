# Commandes pour récuperer les variables d'environnement Vercel
En ligne de commandes:
```bash
git clone --repository vercel--
cd --repository vercel--
npm install vercel
vercel link
vercel env pull .env.local --environment=production
```